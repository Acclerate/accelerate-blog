# ============================================
# Elog 本地同步用的环境变量示例
# ============================================
# 用法：
#   1. 复制本文件为 .elog.env
#   2. 替换下面三个值
#   3. 运行 npm run sync:local
#
# 在 GitHub Actions 上用时，把这三个值配到 Secrets：
#   Settings → Secrets and variables → Actions → New repository secret
# ============================================

# 语雀 Token（头像 → 设置 → Token → 新建，权限可全选）
YUQUE_TOKEN=你的语雀Token

# 语雀 login（个人路径）
# 查看方法：访问你的知识库，URL 形如 https://www.yuque.com/<login>/<repo>
# 例如 https://www.yuque.com/acclerate/blog → login = acclerate
YUQUE_LOGIN=你的语雀login

# 语雀 repo（知识库路径）
# 同上例 → repo = blog
YUQUE_REPO=你的语雀repo
