# ============================================
# Elog 本地同步用的环境变量示例（账号密码模式）
# ============================================
# 用法：
#   1. 复制本文件为 .elog.env
#   2. 替换下面四个值
#   3. 运行 npm run sync:local
#
# ⚠️ .elog.env 千万不要提交到 git（已在 .gitignore 中排除）
# ============================================

# 语雀用户名（一般是手机号，如 13800138000）
YUQUE_USERNAME=你的手机号

# 语雀登录密码
YUQUE_PASSWORD=你的登录密码

# 语雀 login（个人路径）
# 查看方法：访问你的知识库，URL 形如 https://www.yuque.com/<login>/<repo>
# 例如 https://www.yuque.com/acclerate/blog → login = acclerate
YUQUE_LOGIN=你的语雀login

# 语雀 repo（知识库路径）
# 同上例 → repo = blog
YUQUE_REPO=你的语雀repo

# ============================================
# 如果你后续开了语雀超级会员，想用 Token 模式 + CI 自动同步：
#   1. 修改 elog.config.js 把 deploy.type 改回 'yuque'
#   2. 在 GitHub 仓库 Settings → Secrets 配 YUQUE_TOKEN
#   3. 重新启用 .github/workflows/sync-yuque.yml
# ============================================
